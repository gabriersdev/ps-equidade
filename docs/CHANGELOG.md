# Changelog

## [Unreleased]
### Adicionado
- Configuração base do Next.js App Router (Layout e Page principal).
- Integração global com `bootstrap` e `bootstrap.bundle.min.js` (via componente `BootstrapClient`).
- Definição global do esquema de cores corporativas (Verde Escuro, Vermelho, Preto) em `globals.css`.
- Componente base `header.tsx` (Navbar responsiva do Bootstrap).
- Páginas estruturais básicas criadas e mapeadas no Header: `/aulas`, `/calendario`, e `/area-do-aluno`.

### Alterado
- Sobrescrito o estilo visual padrão para forçar remoção de sombras e arredondamento excessivo (bordas quadradas).
